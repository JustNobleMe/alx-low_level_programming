#include "main.h"
#include <unistd.h>
/**
  *_memset- function to print character
  *
  *@s: take value for s
  *@b: takes value for b
  *@n: takes value for n
  *Return: Always 0
  */
char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}
